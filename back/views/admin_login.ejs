<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Login Admin - BiblioLiebre</title>
  <style>
    body { font-family: Arial, sans-serif; background: #f5f5f5; }
    .login-box {
      background: #fff; border-radius: 10px; padding: 30px; max-width: 370px;
      margin: 70px auto; box-shadow: 0 2px 12px rgba(0,0,0,0.07);
    }
    h2 { text-align: center; }
    form { display: flex; flex-direction: column; gap: 14px; }
    label { font-weight: bold; }
    input[type=email],input[type=password] {
      font-size: 16px; padding: 9px; border-radius: 7px; border:1px solid #ccc;
    }
    button { padding: 10px; font-size: 16px; cursor: pointer; border-radius: 6px; background: #1c5afd; color: white; border: none; }
    button:hover { background: #2357d1; }
    .error-msg { color: red; text-align: center; font-size: 15px; margin: 10px 0; }
    .acceso-rapido { padding: 7px 10px; background: #eee; color:#333; border:1px solid #bbb; border-radius: 7px;margin: 5px 0; cursor: pointer; }
  </style>
</head>
<body>
  <div class="login-box">
    <h2>Acceso Administrador</h2>
    <% if (error) { %>
      <div class="error-msg"><%= error %></div>
    <% } %>
    <form action="/admin/login" method="post">
      <label for="email">Correo</label>
      <input type="email" name="email" id="email" placeholder="admin@admin.com" required />
      <label for="password">Contraseña</label>
      <input type="password" name="password" id="password" placeholder="Tu contraseña" required />
      <button type="submit">Ingresar</button>
      <button type="button" class="acceso-rapido" onclick="document.getElementById('email').value='admin@admin.com';document.getElementById('password').value='admin123';">Acceso rápido</button>
    </form>
    <div style="margin-top:18px; text-align: center;">
      <a href="/">Volver al inicio</a>
    </div>
  </div>
</body>
</html>

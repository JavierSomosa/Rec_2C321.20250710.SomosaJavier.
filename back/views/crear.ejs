<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crear Producto - BiblioLiebre</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
        }
        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }
        label {
            font-weight: bold;
        }
        input, select {
            padding: 8px;
            font-size: 16px;
        }
        button {
            padding: 10px;
            background-color: #0f70ee;
            color: white;
            border: none;
            cursor: pointer;
            font-size: 16px;
        }
        button:hover {
            background-color: #45abff;
        }
        a {
            display: inline-block;
            margin-top: 20px;
            color: #0f70ee;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <h1>Crear Nuevo Producto</h1>

    <form action="/crear" method="post" enctype="application/x-www-form-urlencoded">
        <label for="titulo">Título *</label>
        <input type="text" name="titulo" required placeholder="Ej: El principito">

        <label for="tipo">Tipo *</label>
        <select name="tipo" required>
            <option value="">Seleccione un tipo</option>
            <option value="libro">Libro</option>
            <option value="pelicula">Película</option>
        </select>

        <label for="descripcion">Descripción</label>
        <input type="text" name="descripcion" placeholder="Ej: Un clásico sobre un niño y un zorro">

        <label for="precio">Precio *</label>
        <input type="number" name="precio" step="0.01" required placeholder="Ej: 3500">

        <label for="fechaSalida">Fecha de Salida *</label>
        <input type="date" name="fechaSalida" required>

        <label for="image">Ruta de Imagen (opcional)</label>
        <input type="text" name="image" placeholder="Ej: /images/foto-productos/portada-el-principito.jpeg">

        <button type="submit">Crear Producto</button>
    </form>

    <a href="/">← Volver al listado</a>
</body>
</html>
